import { provideClientHydration } from '@angular/platform-browser';
export interface Iproduct {
    id:number;
    name:string;
    price:number;
    quantity:number;
    imgUrl?:string;
    categoryId:number;

    
}

